body {
    background: linear-gradient(135deg, #6B46C1, #4C51BF); /* Purple to Blue gradient from the image */
    margin: 0;
    padding: 20px;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    color: #333;
    min-height: 100vh;
}

h2 {
    color: #4C51BF;
    text-align: center;
    margin-bottom: 20px;
    font-size: 2em;
    text-transform: uppercase;
    letter-spacing: 1px;
}

form {
    width: 80%;
    margin: 0 auto 20px;
    background-color: #fff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    position: relative;
    top: 20px; /* Slightly moved down from top */
    animation: fadeIn 0.5s ease-in-out;
}

@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

label {
    display: block;
    color: #6B46C1;
    font-weight: bold;
    margin-bottom: 5px;
    font-size: 0.9em;
}

input[type="text"], input[type="hidden"], select {
    width: 100%;
    padding: 8px;
    margin-bottom: 10px;
    border: 1px solid #ddd;
    border-radius: 4px;
    box-sizing: border-box;
    transition: border-color 0.3s ease;
}

input[type="text"]:focus, select:focus {
    border-color: #4C51BF;
    outline: none;
    box-shadow: 0 0 5px rgba(76, 81, 191, 0.5);
}

button {
    padding: 10px 15px;
    margin-right: 10px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    background-color: #007bff;
    color: white;
    font-size: 14px;
    transition: background-color 0.3s ease;
}

button[name="action"] {
    background-color: #4C51BF;
}

button[name="action"]:hover {
    background-color: #0056b3;
}

button[name="action"][value="delete"] {
    background-color: #dc3545;
}

button[name="action"][value="delete"]:hover {
    background-color: #c82333;
}

table {
    width: 80%;
    border-collapse: collapse;
    margin: 20px auto;
    background-color: #fff;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    position: relative;
    top: 40px; /* Moved down further for spacing */
}

th, td {
    padding: 12px;
    text-align: left;
    border: 1px solid #ddd;
}

th {
    background-color: #007bff;
    color: white;
}

tr:nth-child(even) {
    background-color: #f8f9fa;
}

tr:hover {
    background-color: #e9ecef;
}

td[colspan="5"] {
    text-align: center;
    color: #888;
    padding: 20px;
}

td button {
    background-color: #E53E3E;
    color: white;
    padding: 8px 15px;
    border-radius: 5px;
    transition: transform 0.3s ease;
}

td button:hover {
    background-color: #C53030;
    transform: scale(1.05);
}

.error-message {
    color: #E53E3E;
    margin-top: 15px;
    font-size: 0.9em;
    background: rgba(229, 62, 62, 0.1);
    padding: 10px;
    border-radius: 5px;
    text-align: center;
    position: relative;
    top: 10px;
}